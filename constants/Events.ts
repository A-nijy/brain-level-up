/**
 * Global event names for DeviceEventEmitter
 */
export const Events = {
    /**
     * Triggered when unread notification count needs to be refreshed
     */
    NOTIFICATIONS_REFRESH: 'NOTIFICATIONS_REFRESH',
};
